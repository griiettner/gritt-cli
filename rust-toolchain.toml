# Pinned so a release can be rebuilt byte for byte later. `cargo` and
# `rustup` read this file automatically; the release scripts and the
# product workflow install exactly this version.
[toolchain]
channel = "nightly-2026-09-03"
components = ["rustfmt", "clippy"]
profile = "minimal"
