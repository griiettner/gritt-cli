.DS_Store
__pycache__/
*.pyc
.agents/.env
.agents/brain/data/
.agents/cli/target/
/target/
/dist/

# `.cargo/config.toml` sets `artifact-dir = "."`, so every built target lands
# at the repository root. Only the committed `gritt` binary belongs here.
/gritt-mcp-fixture
/libgritt_*.rlib
