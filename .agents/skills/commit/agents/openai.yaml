interface:
  display_name: "Commit"
  short_description: "Commit staged changes with a conventional commit message."
  default_prompt: "Use $commit in this repository."
policy:
  allow_implicit_invocation: false
