interface:
  display_name: "Write feature plans"
  short_description: "Writes feature plans and proposals under .agents/plans."
  default_prompt: "Use $write-plan in this repository."
policy:
  allow_implicit_invocation: false
