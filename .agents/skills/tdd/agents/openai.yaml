interface:
  display_name: "Tdd"
  short_description: "Drives behavior-first red-green-refactor implementation."
  default_prompt: "Use $tdd in this repository."
policy:
  allow_implicit_invocation: true
